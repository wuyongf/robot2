<?xml version="1.0"?>
<launch>

  <!-- nw: global parameters -->
  <arg name="gui" default="true" />
  <arg name="tf_prefix" default="" doc="tf_prefix to be used by gazebo plugins and in the robot's urdf etc. //nw: namespace" />

  <!-- nw:global parameters: moving base pos -->
  <arg name="robot_x"   default="0.0" />
  <arg name="robot_y"   default="-2.0" />
  <arg name="robot_yaw" default="0.0" />

  <include file="$(find robot2_gazebo)/launch/robot2_empty_world.launch">
    <arg name="gui" value="$(arg gui)" />
    <arg name="tf_prefix" value="$(arg tf_prefix)" />

    <!-- nw: moving base -->
    <arg name="robot_x" value="$(arg robot_x)" />
    <arg name="robot_y" value="$(arg robot_y)" />
    <arg name="robot_yaw" value="$(arg robot_yaw)" />
  </include>

  <!-- nw: gazebo environment -->
  <include file="$(find robot2_gazebo)/launch/includes/spawn_scene1.launch.xml" />
</launch>

